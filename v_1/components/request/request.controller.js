(function () {
    'use strict';

    angular
        .module('app')
        .controller('RequestController', RequestController);

    RequestController.$inject = ['$location'];
    function RequestController($location) {
        
        var vm = this;

        (function initController() {
            
        })();

    }

})();
